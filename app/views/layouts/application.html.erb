<!DOCTYPE html>
<html>
  <head>
    <title>StudentRegistration</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="turbo-cache-control" content="no-cache">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    
    <%# Autocomplete stylesheet %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@tarekraafat/autocomplete.js@10.2.7/dist/css/autoComplete.02.min.css">

    <%# Autocomplete script %>
    <script src="https://cdn.jsdelivr.net/npm/@tarekraafat/autocomplete.js@10.2.7/dist/autoComplete.min.js"></script>

    <%# Tom select stylesheet for dropdown selection library %>
    <link href="https://cdn.jsdelivr.net/npm/tom-select/dist/css/tom-select.css" rel="stylesheet">

  </head>
  
  <body class="bg-body-secondary" >
    <div class="">
      <div class="position-fixed vh-100">
        <% if user_signed_in? %>
          <header class="vh-100">
            <%= render 'layouts/navigation' %>
              <%#= link_to "Sign out",  destroy_user_session_path, 'data-turbo-method': :delete %>
          </header>
          <%= render "shared/flash_notice" %>
        <% end %>
      </div>

      <div class="" style="<%= 'margin-left: 264px;' if user_signed_in? %>">
        <div class="pt-3 px-5 min-vh-100">
          <%= yield %>
        <div>
        <%= turbo_frame_tag "turbomodal" %>
      </div>
    </div>
  </body>
</html>
