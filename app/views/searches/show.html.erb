<p style="color: green"><%= notice %></p>

<h5 class="" style="">Search results (<%= @search.students.size %>)</h5>

<div class="mt-3 mb-4">
  <%= link_to new_department_search_path(@department), class: "link-underline link-underline-opacity-0" do %>
    <i class="bi bi-arrow-left"></i>
    <span class="fs-5">Back to searches</span>
  <% end %>
</div>


<div class="table-responsive fs-5">
  <table class="table table-sm table-striped align-middle">
    <thead>
      <tr>
        <th scope="col">No.</th>
        <th scope="col">Name</th>
        <th scope="col">Class Year</th>
        <th scope="col">Semester</th>
        <th scope="col"></th>
      </tr>
    </thead>
    <tbody class="accordion" id="accordionExample">
      <% @search.students.each_with_index do |student, index| %>
        <tr class="" style="height: 50px">
          <td><%= index+1 %></td>
          <td><%= student.full_name %></td>
          <td><%= student.class_year %></td>
          <td><%= student.semester %></td>
          <td>
            <%= link_to "Edit", edit_department_student_path(@department, student), class: "btn btn-outline-primary btn-sm" %>
            <%= link_to "Detail", department_student_path(@department, student), class: "btn btn-outline-primary btn-sm" %>
          </td>
        </tr>

      <% end %>
    </tbody>
  </table>
</div>

<% if @search.students.empty? %>
  <%= render partial: "placeholder" %>
<% end %>